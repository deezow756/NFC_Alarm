<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:NFCAlarm"
             x:Class="NFCAlarm.MainPage">

    <StackLayout HorizontalOptions="CenterAndExpand">
        <Label x:Name="txtTime" Text="" FontSize="100" HorizontalOptions="CenterAndExpand" HorizontalTextAlignment="Center"/>
        <ListView x:Name="alarmsList">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ImageCell Tapped="ImageCell_Tapped" ImageSource="{Binding ImageName}" Text="{Binding Name}" Detail="{Binding Time}"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>

</ContentPage>
